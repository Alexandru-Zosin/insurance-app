namespace Domain.Currencies;

internal static class CurrencyConstants
{
    internal const decimal ExchangeRateMinExclusive = 0.0m;

    internal const string InvalidCodeMsg = "Invalid code for currency";
    internal const string InvalidNameMsg = "Invalid name for currency";
    internal const string InvalidExchangeRateMsg = "Invalid exchange rate for currency";
}
