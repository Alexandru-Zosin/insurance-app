namespace Domain.Buildings;

internal static class BuildingConstants
{
    internal const int CityIdMin = 1;

    internal const int ConstructionYearMin = 1600;

    internal const string InvalidAddressMsg = "Invalid address.";
    internal const string InvalidCityIdMsg = "Invalid City Id.";
    internal const string SurfaceAreaMustBePositiveMsg = "Surface area must be positive.";
    internal const string InvalidInsuredValueMsg = "Invalid insured value.";
    internal const string InvalidConstructionYearMsg = "Invalid construction year.";
}
